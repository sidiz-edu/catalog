services:
  app:
    build: .
    ports:
      - "80:80"
    environment:
      - DATABASE_URL=${DATABASE_URL}n6ozbSwPRKW@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres
    restart: unless-stopped
